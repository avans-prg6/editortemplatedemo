@model PrognoseWeekVM

<h4>Wekelijkse Prognose</h4>
<hr />
<form asp-action="PrognoseWeekVM">
      <div class="row">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        </div>
        <div class="row">
            <div class="form-group">
                <label asp-for="StartOfWeek" class="control-label"></label>
                <input asp-for="StartOfWeek" class="form-control" />
                <span asp-validation-for="StartOfWeek" class="text-danger"></span>
            </div>
        </div>
        <div class="row">
            @Html.EditorFor(m => m.PrognoseDays)
        </div>
        <div class="row">
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
